class Constants {
  // Token is static so it can be accessed without an instance
  static String TOKEN = "userDetails";
  static List<dynamic> userDetails = [
    "uid",
    "userFullName",
    "userType",
  ];
}
